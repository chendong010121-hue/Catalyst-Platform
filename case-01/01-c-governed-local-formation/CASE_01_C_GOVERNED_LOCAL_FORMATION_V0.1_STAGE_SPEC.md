# CASE 01-C — GOVERNED LOCAL FORMATION V0.1
## STAGE SPEC — ACCEPTED / EXECUTABLE
### BUILD STAGE · CASE-SCOPED · CASE ↔ PLATFORM CO-EVOLUTION

> **Project:** Catalyst  
> **Case:** CASE 01 — Building Regulation Evidence Agent  
> **Branch:** `case-01`  
> **Parent Stage:** CASE 01-B — `EVIDENCE-BACKED PASS / CLOSED`  
> **Catalyst main baseline:** `5874be1130e8867082880fcd63f659fc909d9efd`  
> **CASE 01 baseline at Stage authoring:** `9efa700eed32cc9891f58fe76c00bf9816252cee`  
> **Implementation author:** DeepSeek  
> **Architecture / Stage authority + external auditor:** ChatGPT  
> **Product / Release authority:** User  
> **CASE 01-C implementation authorization:** **YES — USER AUTHORIZED**  
> **Catalyst `main` mutation:** **FORBIDDEN**  
> **Raw admitted corpus upload:** **FORBIDDEN**

---

# 0. Stage Decision

CASE 01-C is the first real **BUILD** Stage of Case 01.

CASE 01-B already froze the Agent identity, professional purpose, initial obligations, functional decomposition, governed seams, responsibility ownership, legacy adaptation boundary, local corpus permission and immediate formation gaps.

CASE 01-C must now prove:

```text
ACCEPTED GOVERNED DEFINITION
        ↓
MINIMUM CASE-SCOPED BUILDER
        ↓
RUNNABLE DECOMPOSABLE BREA CANDIDATE
        ↓
FORMATION EVIDENCE
        ↓
WHOLE-AGENT PROOF
```

The Stage has two coupled tracks:

```text
TRACK A — CASE
Form the first runnable BREA Candidate.

TRACK B — PLATFORM CO-EVOLUTION
Form only the minimum local Builder / formation mechanism required by Track A.
```

This Stage does **not** admit the Agent into Catalyst and does not prove a generic Builder Platform.

---

# 1. Role Boundary

```text
USER
Stage acceptance, Product / Release decisions, later merge or promotion decisions.

CHATGPT
Architecture planning, Stage Spec, responsibility boundaries, external audit, PASS / REPAIR / STOP decision.

DEEPSEEK
Implementation author: writes Builder, Candidate, tests and evidence; runs verification; creates the authorized CASE 01-C implementation commit and push.
```

DeepSeek must not self-authorize architecture changes, Platform/Runtime changes, CASE 01-D, Capability assetization or merge to `main`.

---

# 2. Mainline — Unchanged

```text
01-A  UNDERSTAND LEGACY                 CLOSED
  ↓
01-B  DEFINE GOVERNED AGENT             CLOSED
  ↓
01-C  GOVERNED LOCAL FORMATION          NOW
  ↓
01-D  ADMISSION / BINDING               NOT AUTHORIZED
  ↓
01-E  RUNTIME EVIDENCE + ASSETIZATION   NOT AUTHORIZED
  ↓
01-F  ONE BOUNDED EVOLUTION             NOT AUTHORIZED
```

Case and Platform advance together, but evidence determines what may later generalize.

---

# 3. Repository / Publication Boundary

Working repository:

```text
chendong010121-hue/agent-runtime
```

Working branch:

```text
case-01
```

Primary allowed implementation path:

```text
case-01/01-c-governed-local-formation/**
```

One accepted-input synchronization exception is permitted **only if the file is missing on the branch**:

```text
case-01/01-b-governed-agent-definition/
builder/BUILDER_CONSUMABLE_DEFINITION_V0.1.md
```

That file must be copied **bit-identically** from the already closed local CASE 01-B package and must have:

```text
SHA256
6c6e4707a3f8b719d6ab9c08cb9e43f337b4cb422bce0d2c22e4b842a9059bb4
```

Do not recreate, summarize, repair or reinterpret it during 01-C.

If unavailable or SHA mismatches:

```text
STOP — ACCEPTED BUILDER INPUT BLOCKER
```

Forbidden:

```text
Catalyst root code changes
Platform Core changes
Runtime changes
main branch changes
merge PR to main
raw corpus commit
```

Publication authorization for DeepSeek:

> After local implementation, tests, evidence and self-audit complete, publish **one implementation commit + one push** to `case-01`.

No intermediate implementation pushes. If a post-push repair is required, STOP and return for new user publication authorization.

---

# 4. Protected Corpus Boundary

Admitted local corpus:

```text
GB55037-2022.md
SHA256 2a217deac98636584dbd328d8449a21bfb4ab30d80483d5355915beaba0594f3

DBJ33T1021-2023.md
SHA256 1296922e3dd7ef209aa8c5cc447e4fdd9a64f37e4f4d403cb8533de8cb31d3f7
```

Status:

```text
LOCAL PILOT ADMITTED
READ ONLY
ORGANIZATIONAL ASSET = NO
UPSTREAM = FORBIDDEN
PERMANENT INHERITANCE = NO
```

Authority:

```text
case-01/01-b-governed-agent-definition/
evidence/LOCAL_CORPUS_REFERENCE_MANIFEST_V0.1.md
```

The Builder must not guess alternate corpus paths.

Raw corpus must never be copied into GitHub. Prefer locator + SHA + assertion over duplicating source text in evidence.

Corpus hash mismatch:

```text
FAIL CLOSED / STOP
```

---

# 5. Accepted Build Target

```text
ID       BREA — Building Regulation Evidence Agent
STATE    CASE 01 Governed Candidate
VERSION  v0.1-candidate
DOMAIN   Building Regulation / Engineering Construction Standards — architecture_pre_design
```

Professional purpose:

> Use project context to provide reliable, applicable, traceable building-regulation evidence for architectural / preliminary design work, and explicitly return uncertainty or fail closed when reliable evidence is unavailable.

Do not put `test Catalyst`, `prove Platform` or `prove Runtime` into the Agent professional purpose.

---

# 6. Accepted Initial Obligations

CASE 01-C must produce conformance evidence for:

```text
OBL-01  Verbatim evidence traceability
OBL-02  Applicability determination
OBL-03  Numeric safety — zero unsupported normative numeric claims
OBL-04  Fail-closed uncertainty
OBL-05  Source fidelity / provenance
OBL-06  Minimum Enterprise attribution
```

Important:

```text
source-code numeric-literal scan
= local formation/build verification

NOT
= OBL-03 public semantics
```

No new public obligation may be invented during 01-C. If one appears necessary: STOP.

---

# 7. Mandatory Functional Decomposition

The Candidate must preserve all eleven accepted function/responsibility boundaries:

```text
FN-01 Question & Context Intake
FN-02 Professional Fact Normalization
FN-03 Regulation Applicability Resolution
FN-04 Evidence Locating & Extraction
FN-05 Evidence Binding & Numeric Safety
FN-06 Uncertainty & Fail-Closed Decision
FN-07 Result Composition & Attribution
FN-08 Artifact & Provenance Preservation
FN-09 Corpus Access & Parsing
FN-10 Provider & Execution Plumbing
FN-11 Local Runner / Service Shell
```

Governance mapping:

```text
FN-01                    DECLARED FUNCTION BOUNDARY
FN-02                    GOVERNED SEAM — SEAM-01
FN-03                    GOVERNED SEAM — SEAM-02
FN-04 / FN-05 / FN-08    GOVERNED SEAM — SEAM-03
FN-06 / FN-07            DECLARED FUNCTION BOUNDARY
FN-09                    PRIVATE IMPLEMENTATION
FN-10 / FN-11            PRIVATE / DEFERRED
```

Hard invariant:

> Every meaningful Agent function remains separately understandable, composable, testable and — where justified — replaceable.

But:

```text
function != microservice
function != Platform resource
function != Skill
function != mandatory class
```

Do not collapse the whole Agent into one Prompt, one RAG pipeline, one class or one module.

---

# 8. Governed Seams

Required:

```text
SEAM-01 Professional Project Facts
SEAM-02 Regulation Applicability
SEAM-03 Regulation Evidence
```

A seam may be represented as an interface, protocol, typed contract, package boundary, pure-function boundary or equivalent testable contract.

Must prove:

```text
stable meaning
explicit dependency direction
replaceable implementation HOW
private implementation cannot redefine seam semantics
```

Do not create Platform Standard objects for these seams in 01-C.

---

# 9. Private Implementation Freedom

DeepSeek / Builder may choose:

```text
retrieval algorithm
chunking / ranking
parser
internal storage / cache
database
provider
prompt wording
internal orchestration
module layout
CLI / local service shape
serialization HOW
```

Prefer the smallest deterministic implementation that proves the architecture.

A model/LLM is **not required** for the first Candidate. Do not add one merely because the historical Agent used one.

Prompt, retrieval and provider must never own Domain meaning, numeric authority or Agent public obligations.

---

# 10. Legacy Adaptation Boundary

Selected for adaptation:

```text
A-02  domain model concepts
A-04  facts lifecycle semantics
A-11  migration-governance manifest pattern
A-12  test patterns
A-13a environment/dependency descriptors
```

Deferred:

```text
A-01 documents/**
A-03 storage schema patterns
A-05 provider boundary pattern
```

Unavailable / forbidden by default:

```text
A-16 index.sqlite / wiki.sqlite / raw PDF set
A-19 knowledge_snapshot
legacy1 app code
legacy .venv
```

Rule:

> Reuse evidence and assets. Do not inherit accidental architecture.

---

# 11. Historical Pilot Reference

`building-regulation-evidence-v0.1` is reference evidence only.

Known behaviors to reproduce at the new whole-Agent boundary:

```text
CASE A
GB 55037-2022 §3.1.3
verbatim evidence / locator / numeric safety

CASE B
DBJ33/T1021-2023 Table 5.0.1 + 5.0.4
project facts → applicability → row/column → evidence-backed numeric conclusion

CASE C
missing material facts
→ insufficient_context / no_reliable_evidence
→ no unsupported professional or numeric conclusion
```

Known OCR frictions may inform private parser tests but do not authorize copying the historical engine architecture.

---

# 12. TRACK B — Minimum Governed Builder

This Stage is the first practical validation of:

# **Agent builds Governed Agent**

Authoritative Builder input:

```text
case-01/01-b-governed-agent-definition/
builder/BUILDER_CONSUMABLE_DEFINITION_V0.1.md
```

The Builder may choose implementation HOW but must not invent missing architecture.

Required logical input:

```text
governed_definition
target_directory
candidate_id
candidate_version
allowed_asset_manifest
corpus_reference_manifest
```

Required logical output:

```text
generated Candidate workspace
function mapping
obligation → test mapping
governed-seam mapping
private-implementation declaration
generated artifact manifest
build report
build evidence
```

Do not create a universal Platform API.

---

# 13. Penguin Harness Reference Mode

Primary engineering reference:

```text
Prism-Shadow/penguin-harness
reference snapshot used in 01-B:
9c895051678e97bdbb05a44b47437e4c2759d771
```

Borrow patterns:

```text
one-sentence requirement UX
Builder → Target Agent
agent-creation
automatic scaffolding
minimal needed Skills / capabilities
structured Agent state concept
runnable generated Agent
validation / reporting
```

Record for later, but do not fully implement now:

```text
benchmark
Evaluator
Optimizer
Snapshot
Candidate N+1
rollback
```

Do not copy as Catalyst authority:

```text
AGENTS.md = professional truth
Penguin Agent State = Catalyst ontology
Penguin Runtime = Catalyst Runtime
Skill = governed seam
Skill = Platform resource
score improvement = governance acceptance
Penguin folder layout = Catalyst Standard
```

Catalyst direction remains:

```text
GOVERNED DEFINITION
→ BUILDER
→ generated implementation artifacts
```

Never let generated Prompt/AGENTS.md reverse-author governed meaning.

---

# 14. Builder Clean-Target Proof

The Builder proof must start from a clean empty Candidate target directory.

Required proof:

```text
INPUT
accepted Builder-consumable definition

BUILDER
minimum Case-scoped formation mechanism

OUTPUT
BREA v0.1 Candidate

VALIDATION
all accepted functions, obligations and seam mappings exist
without new architecture being invented
```

Bounded repair is allowed only when:

```text
Governed Definition does not change
repair is recorded
changed generated files are listed
defect is classified
Builder protocol is corrected if systemic
```

Defect classes:

```text
MECHANICAL
INTERPRETATION
ARCHITECTURE
ENVIRONMENT
```

ARCHITECTURE defect → STOP.

If repeated manual architecture redesign is required, GAP-05 is not closed.

---

# 15. Required Workspace

```text
case-01/01-c-governed-local-formation/
├── CASE_01_C_GOVERNED_LOCAL_FORMATION_V0.1_STAGE_SPEC.md
├── README.md
├── builder/
│   ├── BUILDER_PROTOCOL_V0.1.md
│   ├── BUILDER_REQUEST_V0.1.*
│   ├── BUILDER_OUTPUT_MANIFEST_V0.1.*
│   └── BUILDER_RUN_REPORT_V0.1.md
├── candidate/
│   └── brea-v0.1/
│       ├── README.md
│       ├── <implementation>
│       └── <tests>
├── evidence/
│   ├── FORMATION_EVIDENCE_INDEX_V0.1.md
│   ├── FUNCTION_CONFORMANCE_V0.1.md
│   ├── OBLIGATION_CONFORMANCE_V0.1.md
│   ├── GOVERNED_SEAM_CONFORMANCE_V0.1.md
│   ├── LEGACY_ADAPTATION_TRACE_V0.1.md
│   ├── BUILDER_FORMATION_TRACE_V0.1.md
│   ├── PLATFORM_GAP_UPDATE_V0.1.md
│   └── CASE_RESULTS/
├── findings/
│   └── CASE_01_C_FINDINGS_AND_UNKNOWNS_V0.1.md
└── review/
    ├── CASE_01_C_EXECUTION_REPORT_V0.1.md
    ├── CASE_01_D_ENTRY_BOUNDARY_V0.1.md
    └── CATALYST_INTEGRITY_CHECK_V0.1.txt
```

No raw corpus under this directory.

---

# 16. Candidate Input / Result Boundary

Minimum Candidate input:

```text
request_id
question
project_context
regulation_context
enterprise_context
```

Minimum Enterprise attribution:

```text
organization_id
user_id
optional project_id
```

Enterprise metadata must not participate in professional regulation meaning.

Candidate result must provide a stable Case-local equivalent of:

```text
RegulationEvidenceResult
request_id
status
conclusion
evidence_items[]
artifacts[]
uncertainty
implementation_metadata
```

Evidence item must support:

```text
source_identity
source_title
source_version_or_date
locator
evidence_type
evidence_content or safe evidence reference
claim_relation
```

This is **not** a new Platform Standard contract.

---

# 17. Minimum Professional Formation Tests

At least three assembled whole-Agent cases are required.

## T-C01 — Direct Clause

Reference intent:

```text
GB 55037-2022 §3.1.3
```

Must prove:

```text
verbatim / faithful evidence
source identity
locator
claim relation
numeric safety
accepted_with_evidence or equivalent
```

No conclusion value may be hardcoded as normative authority.

## T-C02 — Conditional Table

Reference intent:

```text
project facts
→ applicability
→ DBJ33/T1021-2023 Table 5.0.1
→ Table 5.0.4
→ evidence-backed selected row/column conclusion
```

Must prove composition:

```text
SEAM-01 → SEAM-02 → SEAM-03
```

## T-C03 — Fail Closed

Omit material project facts.

Expected:

```text
insufficient_context
or no_reliable_evidence

no unsupported professional conclusion
no unsupported numeric value
explicit missing facts / uncertainty
```

Tests may not pass merely by matching hardcoded expected answer text.

---

# 18. Structural / Architecture Tests

Required:

```text
ST-01  FN-01..FN-11 all mapped; none disappears.
ST-02  SEAM-01..03 explicit and testable.
ST-03  dependency direction does not make private vendor/provider schema semantic authority.
ST-04  Prompt wording alone cannot redefine facts/applicability/numeric authority/public obligations.
ST-05  Enterprise ID changes do not alter professional applicability/evidence.
ST-06  every normative numeric conclusion traces to admitted source evidence.
ST-07  corpus SHA mismatch fails closed.
ST-08  diff outside authorized Case paths is empty.
```

A local source-code literal audit may supplement ST-06; it is not the professional obligation itself.

---

# 19. Required Formation Evidence

```text
FE-01 Builder input path + SHA
FE-02 Builder output manifest
FE-03 Agent identity/version
FE-04 FN-01..11 implementation mapping
FE-05 SEAM-01..03 mapping + tests
FE-06 OBL-01..06 conformance mapping
FE-07 legacy adaptation trace A-02/A-04/A-11/A-12/A-13a
FE-08 Domain / Enterprise separation proof
FE-09 corpus hash proof
FE-10 direct-clause result
FE-11 conditional-table result
FE-12 fail-closed result
FE-13 numeric-safety proof
FE-14 Prompt non-authority proof
FE-15 no Platform/Runtime/root contamination proof
FE-16 Builder formation trace
```

Every evidence item must point to a reproducible file, test or run.

Unit tests alone are insufficient; whole-Agent formation must pass.

---

# 20. Platform Gap Handling

Immediate 01-C gaps from accepted 01-B:

```text
GAP-01 Builder-consumable Governed Agent definition
GAP-05 Minimum local Builder capability
```

Attempt to close them **for Case 01 only**.

Allowed end states:

```text
CASE-CLOSED
GENERALIZATION-CANDIDATE
STILL-OPEN
REJECTED
```

Do not call a Case-closed mechanism a Catalyst Platform capability without later generalization evidence.

For any new gap record:

```text
GAP ID
Case need
observed friction/failure
responsibility owner
local solution
reference pattern
evidence
generalization status
```

Default status:

```text
LOCAL ONLY
```

---

# 21. Explicitly Deferred

Do not build:

```text
Platform admission / binding
new Platform Standard
Platform Core schema
new Runtime
generic Agent Builder Platform
enterprise IAM / approval / policy engine
workflow engine
full benchmark platform
Optimizer
self-evolution
production snapshot/rollback tooling
Capability assetization
multi-Agent organization
```

These require later evidence-driven stages.

---

# 22. Preflight

Before implementation verify:

```text
P-01 branch == case-01
P-02 accepted CASE 01-B closure present
P-04 corpus reference manifest present
P-05 local corpus files exist
P-06 corpus SHA256 matches manifest
P-07 Catalyst main baseline is known and unchanged unless later explicitly accepted
P-08 no unauthorized Catalyst-root changes
P-09 target 01-C workspace contains no stale unreviewed implementation
```

## P-03 — Accepted Builder Input

`BUILDER_CONSUMABLE_DEFINITION_V0.1.md` must be available.

Preferred: already on branch.

If absent, DeepSeek may locate the exact accepted local copy from the closed CASE 01-B package and must verify:

```text
SHA256
6c6e4707a3f8b719d6ab9c08cb9e43f337b4cb422bce0d2c22e4b842a9059bb4
```

Use that exact file as Builder input and stage the bit-identical file in the final 01-C publication. No semantic edit is permitted.

Failures:

```text
P-03 fail → STOP — ACCEPTED BUILDER INPUT BLOCKER
P-05/P-06 fail → STOP — CORPUS INPUT BLOCKER
unauthorized repo changes → STOP — WORKTREE / BRANCH BLOCKER
```

Do not auto-clean unknown user work.

---

# 23. Legal Execution Sequence

DeepSeek is authorized to start now:

```text
C0  Git / corpus / accepted-input preflight
 ↓
C1  Freeze Builder input snapshot + hashes
 ↓
C2  Implement minimum Case-scoped Builder protocol
 ↓
C3  Run Builder into a clean Candidate target
 ↓
C4  Validate function / seam / obligation mappings
 ↓
C5  Perform bounded recorded mechanical repair if necessary
 ↓
C6  Complete the deterministic first BREA Candidate
 ↓
C7  Run structural / architecture tests
 ↓
C8  Run T-C01 / T-C02 / T-C03 through assembled Agent
 ↓
C9  Generate Formation Evidence Index
 ↓
C10 Update Platform Gap evidence
 ↓
C11 Run whole-Agent formation review
 ↓
C12 Write CASE 01-D Entry Boundary — design boundary only
 ↓
C13 Final repo / main contamination check
 ↓
C14 Local self-audit
 ↓
C15 One commit + one push to case-01
 ↓
STOP
→ EXTERNAL REVIEW BY CHATGPT
```

CASE 01-D is not automatically authorized.

---

# 24. STOP Conditions

STOP immediately if:

```text
S-01 Agent professional purpose must change.
S-02 new public obligation is required.
S-03 FN-01..11 decomposition is no longer workable.
S-04 SEAM-01..03 ownership must change.
S-05 Enterprise meaning must enter Domain semantics.
S-06 Domain meaning must enter Runtime.
S-07 Platform Core change appears required.
S-08 Runtime change appears required.
S-09 Builder must invent missing architecture.
S-10 Builder writes outside authorized Case paths.
S-11 raw corpus is about to be committed.
S-12 corpus SHA mismatches.
S-13 A-16/A-19 becomes necessary.
S-14 Legacy Agent architecture is copied wholesale.
S-15 Prompt becomes professional truth authority.
S-16 unsupported normative numeric value is emitted.
S-17 admission / binding implementation begins.
S-18 generic Builder Platform is being built before Case proof.
S-19 Catalyst root or main is modified.
S-20 blocking architecture unknown is hidden as implementation assumption.
```

On STOP:

```text
preserve evidence
record finding
do not widen scope
do not push partial implementation
return for architecture / user review
```

---

# 25. Builder Success Criteria

The minimum Builder passes Case 01 formation only if:

```text
BC-01 consumes accepted governed definition.
BC-02 does not require manual re-invention of Agent architecture.
BC-03 creates a new Candidate in clean target directory.
BC-04 produces all required function mappings.
BC-05 preserves governed seam ownership.
BC-06 preserves private implementation freedom.
BC-07 does not copy raw corpus.
BC-08 does not edit Catalyst root / Platform / Runtime.
BC-09 produces build manifest + report.
BC-10 Candidate reaches formation pass with only bounded recorded mechanical repair.
```

This proves a **Case-scoped Builder capability**, not a generic Catalyst Builder Platform.

---

# 26. BREA Candidate Success Criteria

```text
AC-01 professional purpose preserved.
AC-02 OBL-01..06 represented and tested.
AC-03 FN-01..11 accounted for.
AC-04 SEAM-01..03 explicit and testable.
AC-05 Prompt does not own professional truth.
AC-06 retrieval/provider does not own Domain semantics.
AC-07 Enterprise attribution remains orthogonal.
AC-08 direct-clause whole-Agent case passes.
AC-09 conditional-table whole-Agent case passes.
AC-10 fail-closed whole-Agent case passes.
AC-11 unsupported normative numbers = 0.
AC-12 corpus hash mismatch fails closed.
AC-13 evidence locator/provenance is reproducible.
AC-14 legacy adaptation trace complete.
AC-15 raw corpus committed = NO.
AC-16 Platform / Runtime changes = NONE.
AC-17 whole Agent runnable from documented local command.
AC-18 Builder → Candidate trace preserved.
```

---

# 27. Whole-Agent Formation Gate

CASE 01-C cannot pass only because unit tests pass.

Required assembled proof:

```text
request
→ FN-01
→ governed/private function composition
→ FN-07
→ RegulationEvidenceResult
```

At least one direct clause case, one conditional table case and one fail-closed case must pass through the real assembled Candidate.

This proves formation, not enterprise production readiness.

---

# 28. Required Output Package

At Stage completion, GitHub must contain under:

```text
case-01/01-c-governed-local-formation/
```

at minimum:

```text
README.md
builder/BUILDER_PROTOCOL_V0.1.md
builder/BUILDER_REQUEST_V0.1.*
builder/BUILDER_OUTPUT_MANIFEST_V0.1.*
builder/BUILDER_RUN_REPORT_V0.1.md
candidate/brea-v0.1/<runnable implementation + tests + README>
evidence/FORMATION_EVIDENCE_INDEX_V0.1.md
evidence/FUNCTION_CONFORMANCE_V0.1.md
evidence/OBLIGATION_CONFORMANCE_V0.1.md
evidence/GOVERNED_SEAM_CONFORMANCE_V0.1.md
evidence/LEGACY_ADAPTATION_TRACE_V0.1.md
evidence/BUILDER_FORMATION_TRACE_V0.1.md
evidence/PLATFORM_GAP_UPDATE_V0.1.md
evidence/CASE_RESULTS/**
findings/CASE_01_C_FINDINGS_AND_UNKNOWNS_V0.1.md
review/CASE_01_C_EXECUTION_REPORT_V0.1.md
review/CASE_01_D_ENTRY_BOUNDARY_V0.1.md
review/CATALYST_INTEGRITY_CHECK_V0.1.txt
```

No raw corpus.

---

# 29. CASE 01-D Entry Boundary

01-C may prepare, but not execute, `CASE_01_D_ENTRY_BOUNDARY_V0.1.md`.

It must answer only:

```text
What exact Agent Candidate exists?
Which obligations are formation-proven?
Which seams are implementation-proven?
What local Builder capability is proven?
Which Platform gaps remain?
What must Admission / Binding prove next?
What must not be generalized yet?
What Case-specific dependencies remain?
What evidence is still missing for organizational adoption?
```

Do not implement admission/binding in 01-C.

---

# 30. Acceptance Gate

CASE 01-C can close only if all pass:

```text
CG-01 accepted Builder input used.
CG-02 clean-target Builder generation proven.
CG-03 Builder invented no architecture.
CG-04 BREA purpose preserved.
CG-05 OBL-01..06 conformance passes.
CG-06 FN-01..11 explicitly accounted for.
CG-07 SEAM-01..03 testable.
CG-08 private HOW remains replaceable.
CG-09 Domain / Enterprise separation passes.
CG-10 Prompt / RAG / provider do not own long-lived meaning.
CG-11 T-C01 passes.
CG-12 T-C02 passes.
CG-13 T-C03 passes.
CG-14 unsupported normative numeric claims = 0.
CG-15 corpus hashes match; raw corpus uncommitted.
CG-16 legacy adaptation trace complete.
CG-17 Builder formation trace complete.
CG-18 Platform Gap Update is evidence-backed.
CG-19 Platform Core / Runtime / main mutation = NONE.
CG-20 whole-Agent formation run passes.
CG-21 01-D Entry Boundary is explicit.
CG-22 Stage ends STOP / EXTERNAL REVIEW.
```

---

# 31. Final Execution Report Format

DeepSeek final report must include:

```text
CASE 01-C STATUS
PASS / STOPPED / FAIL

CASE BRANCH
case-01

CASE COMMIT
<sha or NONE>

BREA CANDIDATE
path / version

BUILDER INPUT
path / SHA256

BUILDER CLEAN-TARGET GENERATION
PASS / FAIL

BOUNDED REPAIRS
N / list

FUNCTIONS
FN-01..FN-11 PASS / exceptions

GOVERNED SEAMS
SEAM-01..03 PASS / exceptions

OBLIGATIONS
OBL-01..06 PASS / exceptions

DIRECT CLAUSE CASE
PASS / FAIL

CONDITIONAL TABLE CASE
PASS / FAIL

FAIL-CLOSED CASE
PASS / FAIL

UNSUPPORTED NORMATIVE NUMBERS
0 / N

CORPUS HASH
PASS / FAIL

RAW CORPUS COMMITTED
NO / YES

LEGACY ADAPTATION
A-02/A-04/A-11/A-12/A-13a status

PLATFORM GAPS CLOSED FOR CASE
IDs / NONE

NEW GAPS
IDs / NONE

PLATFORM CORE CHANGE
NONE / details

RUNTIME CHANGE
NONE / details

CATALYST ROOT CHANGE
NONE / details

MAIN CHANGE
NONE / details

CASE 01-D
NOT AUTHORIZED

FINAL
READY FOR CASE 01-C EXTERNAL REVIEW
or
STOPPED — USER / ARCHITECTURE DECISION REQUIRED
```

---

# 32. Final Stage Status

# **CASE 01-C — AUTHORIZED FOR DEEPSEEK EXECUTION**

The target proof is:

```text
Governed Definition
→ Minimum Governed Builder
→ Decomposable BREA Candidate
→ Formation Evidence
→ Whole-Agent Proof
```

with:

```text
no architecture invention
no raw corpus upstream
no Catalyst main contamination
no premature Platformization
```

Success does **not** mean:

```text
Agent admitted into Catalyst
Capability assetized
generic Builder Platform proven
production-ready enterprise Agent
self-evolution proven
```

Execution may begin immediately from this GitHub Stage Spec.

Exit:

```text
STOP
→ CASE 01-C EXTERNAL REVIEW BY CHATGPT
→ explicit user decision
→ only then CASE 01-D
```
