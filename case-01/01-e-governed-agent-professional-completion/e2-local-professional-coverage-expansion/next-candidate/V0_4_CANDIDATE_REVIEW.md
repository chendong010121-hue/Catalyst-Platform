# BREA v0.4 Candidate Review

Status: `FORMED` and ready for the bounded freeze commit. This is construction
evidence, not E2-C authorization, admission, binding, or a claim of final product
completion.

## Identity and lineage

`case-01.brea @ 0.4-candidate` descends from the frozen
`case-01.brea @ 0.3-candidate`, using v0.2 generalized local-query behavior, v0.3
professional coverage, and the F-EXP-03R A' minimum semantic direction. The build
was performed from synchronized HEAD `db62e13d40914465b0ea557cf99dfb676b5bcad6`.
Candidate tree SHA-256:
`6e82144877242a2bef87236a5ea63887474c426e675635c8da8166990081c430`.

## Construction result

C-01 through C-10 all pass. The product path is:

```text
Question → Fact Normalization → Local Evidence Retrieval → G-BASE Evidence Unit
→ Generic Semantic Derivation → Ephemeral Semantic View → SEAM-02 Applicability
→ Deterministic Verification → Evidence-bound Answer / Fail Closed
```

The A' semantic view is private and ephemeral. A typed RegulationUnit is not
persisted. Applicability has an observable `SEAM-02` owner, positive scope is
required, and material conditions remain distinct.

## Professional contract and regressions

PC-01..PC-07 pass on the supported professional path. T-C01, T-C02, and T-C03
pass with their preserved statuses. E1 generalized local-query regression passes.
The inherited v0.3 construction suite passes.

The derived numeric trace records source operand `1500`, source modifier
`增加1.0倍`, runtime formula `1500.0 * 2.0`, and runtime result `3000.0`.
The table trace preserves source row values `0. 8`, `1. 1`, `1.1`, `1.0`, `1.8`
and binds selected value `1.1` from the source-backed row.

Positive applicability rejects a non-public building as unresolved rather than
applying the rule by absence of an exclusion. Underground equipment and other
underground areas remain distinct and resolve to source-backed `1000` and `500`
respectively. Missing professional facts fail closed without a numeric conclusion.

## Growth gate

`PENDING`. No additional legitimate provenance-backed source or source revision was
available during this build. No source was fabricated; no separate F-EXP-01 was run.

## Boundaries

The candidate only changes the authorized `next-candidate` surface. v0.1, v0.2,
v0.3, Platform Core, Runtime, RuntimeAdapter, Enterprise extensions, and main are
unchanged. No E2-C benchmark, admission, binding, new Seam/Obligation, raw corpus
upstream, LLM, dense retrieval, embeddings, vector DB, Web, Memory, or multi-agent
mechanism was introduced.

The intended publication state is `FROZEN / NOT ADMITTED / NOT BOUND`. External
Candidate Freeze Review remains required.
