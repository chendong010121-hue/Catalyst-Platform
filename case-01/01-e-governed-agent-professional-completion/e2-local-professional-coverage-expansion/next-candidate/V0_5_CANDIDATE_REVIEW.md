# BREA v0.5 Candidate Review

`case-01.brea @ 0.5-candidate` is formed from the frozen v0.4 candidate and repairs only the four targeted external-freeze findings. The v0.4 tree remains untouched.

## Observed result

- R-01 passed: semantic derivation extracts reusable numeric modifiers from retrieved evidence. `增加 1.0 倍` became factor `2.0`; `提高 30%` became factor `1.3`. Modifier conditions are represented in the ephemeral semantic view and resolved by SEAM-02; no professional-family branch remains in generic semantic code.
- R-02 passed: professional route selection iterates declarative `professional_data` route declarations. Generic execution dispatches only on declared route kind; family names, triggers, source ids, and locators are not execution literals.
- R-03 passed: the strengthened allowlisted Domain-data scan found no route/family names, professional trigger phrases, normative literals/modifiers, or source/locator routing in `brea` Python execution code.
- R-04 passed: five-form proof is limited to the forms actually executed. Direct clause, conditional rule, table rule, scope plus exception/exclusion, and derived numeric modifier all report `path=generic_professional`.

The derived trace observed `1500.0 * 2.0 = 3000.0`. The table trace observed source row values including `0.8`, `1.1`, `1.1`, `1.0`, `1.8` and selected `1.1`; the conclusion was not sourced from a project-specific Gold literal.

## Preserved boundaries

T-01..T-09, PC-01..PC-07, E1, T-C01/T-C02/T-C03, source fidelity, provenance, numeric traceability, table fidelity, FN-01..FN-11, SEAM-01..SEAM-03, OBL-01..OBL-06, and platform-bound execution all passed. The preserved local source references remained SHA-verified. No new source was available, so Growth Gate is `PENDING`.

Only the new `candidate/brea-v0.5/**` surface and the three v0.5 artifacts are formed. v0.1/v0.2/v0.3/v0.4, raw corpus, Platform Core, Runtime, RuntimeAdapter, Enterprise extensions, and `main` remain outside the implementation scope.

## Freeze boundary

Status: `FROZEN / NOT ADMITTED / NOT BOUND`.

This record does not authorize E2-C, Admission, Binding, or Platform promotion. External Candidate Freeze Review remains the next gate.
