# CASE 01-E / E2 — LOCAL PROFESSIONAL COVERAGE EXPANSION V0.1
## STAGE SPEC — FIRST INTENTIONAL APPLICATION OF THE ACCEPTED CONSTRUCTION METHOD
### PROFESSIONAL COVERAGE · CANDIDATE N+1 · CANDIDATE FREEZE · INDEPENDENT EVALUATION · METHOD VALIDATION

> **Project:** Catalyst  
> **Case:** CASE 01 — Building Regulation Evidence Agent (BREA)  
> **Branch:** `case-01`  
> **E1 reference commit:** `fff6629f8ab6015dea89ca68a6fc43feee32fad0`  
> **Accepted Development Method:** `case-01/methods/CATALYST_GOVERNED_AGENT_CONSTRUCTION_METHOD_V0.1_ACCEPTED.md`  
> **Accepted Method commit:** `a5e7b0b9384f8108d5af22bfe9fea317d8e60cee`  
> **E2 Stage Plan commit:** `b0ddce6787c6b9d6b23da3b48672fc719ac80093`  
> **Accepted Catalyst `main`:** `5874be1130e8867082880fcd63f659fc909d9efd`  
> **Current product Reference:** `case-01.brea @ 0.2-candidate`  
> **E1:** `EVIDENCE-BACKED PASS / CLOSED`  
> **Method state:** `METHOD ACCEPTED FOR CASE 01 USE / CASE-DERIVED / CASE-EVIDENCED`  
> **Method full validation:** `PENDING INTENTIONAL APPLICATION`  
> **First intentional validation Stage:** `CASE 01-E / E2`  
> **Stage authority + external reviewer:** ChatGPT  
> **Implementation / evidence executor:** DeepSeek  
> **Product / Stage / Release authority:** User  
> **E2 Stage Spec:** **ACCEPTED**  
> **E2-AB execution:** **REQUIRES EXPLICIT USER AUTHORIZATION**  
> **E2-C execution:** **REQUIRES A SECOND EXPLICIT USER AUTHORIZATION AFTER CANDIDATE FREEZE REVIEW**  
> **BREA v0.3 Admission / Binding:** **NOT AUTHORIZED**  
> **Later E3 / Web supplement:** **NOT AUTHORIZED**  
> **Platform Core / Runtime / main mutation:** **FORBIDDEN**

---

# 0. Stage Thesis

E1 proved that BREA v0.2 can perform generalized deterministic local evidence retrieval over the two currently admitted local corpora without reverting to benchmark-specific runtime branches.

E2 must prove the next, harder product capability:

> **BREA can expand into materially broader professional regulation coverage — not merely broader text retrieval — while preserving governed responsibility boundaries, source fidelity, numeric safety, fail-closed behavior, Candidate lineage, Platform compatibility and independent evaluation integrity.**

E2 is also the first Stage intentionally executed under the accepted:

```text
CATALYST_GOVERNED_AGENT_CONSTRUCTION_METHOD_V0.1
```

Therefore E2 has two success targets:

```text
PRODUCT SUCCESS
BREA gains one genuinely new bounded professional question / applicability family.

METHOD SUCCESS
The applicable P-01 → P-09 Method chain is intentionally executed and externally reviewable.
```

E2 does not automatically validate the Method.

Only a separate external Method review after successful E2 may upgrade the Method to:

```text
CASE 01 VALIDATED METHOD
```

---

# 1. Stage Structure — Two Execution Gates

E2 is one Stage with two independently authorized execution phases.

```text
E2-AB
PROFESSIONAL COVERAGE SELECTION
+
METHOD P-01..P-06 DESIGN EVIDENCE
+
GOVERNED CANDIDATE N+1 CONSTRUCTION
+
CANDIDATE FREEZE

        ↓

PUBLICATION GATE 1
one E2-AB Candidate-Freeze commit + push

        ↓

CHATGPT EXTERNAL CANDIDATE-FREEZE REVIEW

        ↓ PASS + NEW USER AUTHORIZATION

E2-C
POST-FREEZE EVALUATION CONTRACT / BENCHMARK
+
FROZEN-CANDIDATE PROFESSIONAL EVALUATION
+
P-07 / P-08 PACKAGE / P-09 GENERALIZATION EVIDENCE

        ↓

PUBLICATION GATE 2
one E2-C evaluation-evidence commit + push

        ↓

CHATGPT FINAL E2 EXTERNAL REVIEW
```

This separation is mandatory.

It exists to prove:

```text
Candidate implementation
was frozen before
independent Benchmark creation / reveal
```

and to avoid repeating the E1 same-commit chronology weakness.

---

# 2. Protected References

The following are immutable references during E2:

```text
BREA v0.1-candidate
ADMITTED / BOUND / READ-ONLY

BREA v0.2-candidate
E1 ACCEPTED CANDIDATE BASELINE
GENERALIZED LOCAL EVIDENCE QUERY PROVEN
NOT ADMITTED / NOT BOUND

Catalyst main
5874be1130e8867082880fcd63f659fc909d9efd
```

E2 must not modify v0.1 or v0.2 in place.

The new Candidate identity is now frozen as:

```text
agent_id:
case-01.brea

candidate_version:
0.3-candidate
```

Expected lineage:

```text
case-01.brea @ 0.2-candidate
        ↓ governed professional change
case-01.brea @ 0.3-candidate
```

BREA v0.3 is not admitted and not bound by E2.

---

# 3. E2 Product Scope — Professional Coverage, Not Corpus Count

E2 must select exactly one bounded new professional coverage slice.

The selected slice must be clearly outside the existing narrow professional applicability families proven by:

```text
T-C01 fire-distance path
T-C02 parking allocation path
T-C03 insufficient-context regression
```

The selected E2 slice must add:

```text
NEW PROFESSIONAL QUESTION FAMILY
+
NEW PROFESSIONAL APPLICABILITY / DECISION BEHAVIOR
or
NEW EXPLICIT NON-APPLICABILITY DECISION BEHAVIOR
```

A new clause retrieval alone is not sufficient.

A third corpus file alone is not sufficient.

The selected slice must produce observable professional behavior beyond E1 retrieval.

Eligible examples, only if supported by real local evidence:

```text
fire-safety applicability beyond fire-distance
parking / vehicle-access applicability beyond allocation table
new building-regulation source with a new professional applicability family
new conditional-rule family requiring additional project facts
cross-clause applicability inside current admitted corpora
```

The Stage must not preselect a slice merely because it is easy to implement.

---

# 4. Source Strategy

E2 may follow either of two valid source paths.

## SOURCE PATH A — Existing admitted corpora

Use the two already admitted local sources if they contain enough authoritative evidence for a materially new professional coverage family.

## SOURCE PATH B — One new local professional source

If a new source is genuinely required, E2 may propose exactly the minimum new local source set necessary for the selected slice.

Every new source must pass the E2 Case-local source gate before Candidate construction.

Minimum source record:

```text
source_id
formal title / identifier
professional scope
source provenance
local file reference
SHA256
format / OCR quality notes
read-only boundary
raw-corpus upstream status
known professional limitations
why this source is necessary for the selected E2 slice
```

Default source status:

```text
CASE-LOCAL PROFESSIONAL PILOT SOURCE
READ-ONLY
RAW CORPUS NOT COMMITTED
NOT PLATFORM ASSET
NOT ORGANIZATIONAL ASSET
NO PERMANENT INHERITANCE
```

If provenance cannot be established:

```text
DO NOT ADMIT SOURCE
```

If no trustworthy source supports the selected professional slice:

```text
STOP E2-AB
→ INSUFFICIENT PROFESSIONAL EVIDENCE
```

Do not lower the source standard merely to create v0.3.

---

# 5. Explicitly Deferred Product Scope

E2 does not automatically include:

```text
Web fallback
Internet search
official-site verification
URL supplementation
RAG
vector database
LLM
Agent loop
Memory
multi-turn UX
frontend
backend service productization
IAM / RBAC
Enterprise policy engine
```

Any such technology or capability may enter E2 only if:

```text
the selected E2 professional problem cannot be solved within the current deterministic/local mechanism
+
the exact capability gap is demonstrated
+
the technology selection gate justifies it
+
it remains within E2's protected architecture boundaries
```

If Web access is required for the product behavior itself:

```text
STOP
```

Web supplement belongs to a later separately reviewed slice unless architecture authority explicitly changes the roadmap.

---

# 6. Accepted Development Method — Binding Input

E2 must intentionally apply the accepted Method as a development-method input.

Required applicable chain:

```text
P-01 Problem Extraction
P-02 Mechanism Extraction
P-03 Assumption Extraction
P-04 Responsibility Classification
P-05 Catalyst Compatibility Review
P-06 Catalyst-Native Reconstruction
P-07 Case-Local Proof
P-08 Evidence Review
P-09 Generalization Gate
```

Method authority remains subordinate to:

```text
Governing Baseline / Architecture
>
E2 Stage Spec / User authorization
>
Accepted Method
>
Case-local implementation choice
```

The Method does not authorize implementation.

---

# 7. Required External Mechanism for Method Validation

E2 intentionally applies one already researched external mechanism:

# **Candidate Freeze → Independent Evaluation Isolation**

Pinned research source:

```text
Repository:
Prism-Shadow/penguin-harness

commit:
8e4a77d492a0033e45ac799a681216ed703facac

file:
packages/skills/skills/benchmark-design/SKILL.md

blob SHA:
3ed641c47ab2072d3424a6863857ebe7e97459b5
```

Catalyst uses the mechanism, not Penguin architecture.

Mechanism to learn:

```text
capability/evaluation contract
→ freeze Candidate identity
→ separate evaluation identity
→ design/reveal evaluation after Candidate freeze
→ preserve exact evaluation revision
→ detect leakage
→ retain traceable decision evidence
```

Catalyst-native reconstruction:

```text
E2 Evaluation Contract
→ frozen BREA v0.3 identity / fingerprint
→ post-freeze professional Benchmark
→ frozen Benchmark identity
→ professional evaluation evidence
→ governance decision evidence
```

Explicitly not inherited:

```text
Penguin Agent State
Penguin Skill model
Penguin Benchmark directory schema
Penguin Scoreboard
Penguin evaluator protocol
Penguin Runtime
Penguin identity model
```

No external repo execution is required for this already-pinned reference.

Additional external mechanisms may only be researched if a demonstrated E2 problem requires them.

---

# 8. E2-AB — P-01 Problem Extraction

Before Candidate implementation, create:

```text
method/P01_E2_PROBLEM_RECORD_V0.1.md
```

Required fields:

```text
real user / professional problem
professional context
current v0.2 limitation
target user
input class
observable desired professional behavior
current failure mode
why current behavior is professionally insufficient
technology-neutral statement
source evidence needed
```

The problem must be stated independently of implementation technology.

Invalid:

```text
We need RAG.
```

Valid form:

```text
BREA cannot reliably determine X professional applicability from Y project facts and local normative evidence.
```

---

# 9. E2-AB — Professional Coverage Selection

Create:

```text
professional/E2_PROFESSIONAL_COVERAGE_SELECTION_V0.1.md
```

It must compare at least 2 realistic candidate professional slices when evidence allows, then select one.

Required decision criteria:

```text
professional value
local authoritative evidence availability
difference from existing T-C01/T-C02 family
project facts required
applicability complexity
evidence type
source quality / OCR risk
implementation risk
evaluation feasibility
```

Final selected slice must state:

```text
SELECTED E2 PROFESSIONAL FAMILY

WHY NOW

WHY NOT RETRIEVAL-ONLY

SOURCE PATH
A existing admitted corpus
or
B new locally admitted source

SUCCESS BEHAVIOR

FAIL-CLOSED BEHAVIOR
```

---

# 10. E2-AB — P-02 Mechanism Extraction

Create:

```text
method/P02_E2_MECHANISM_ABSTRACTION_V0.1.md
```

At minimum include the benchmark-isolation mechanism:

```text
problem solved
mechanism abstraction
observable benefit
failure mode
source reference
what is deliberately NOT inherited
```

Do not use product-specific Penguin vocabulary as Catalyst architecture.

---

# 11. E2-AB — P-03 Assumption Extraction

Create:

```text
method/P03_E2_ASSUMPTION_REGISTER_V0.1.md
```

At minimum examine:

```text
benchmark designer knows Candidate implementation
benchmark and Candidate live in same repo
all evaluation cases may be public
evaluation runtime is deterministic
local corpus is professionally clean
OCR text equals normative source
retrieval success equals professional applicability
selected professional facts are complete
one successful source is universally authoritative
```

Classify each:

```text
COMPATIBLE
CASE-LOCAL
REQUIRES ADAPTATION
INCOMPATIBLE
UNKNOWN
```

Unknown professional assumptions must not silently become normative behavior.

---

# 12. E2-AB — P-04 Responsibility Classification

Create:

```text
method/P04_E2_RESPONSIBILITY_CLASSIFICATION_V0.1.md
```

Map every material E2 change across:

```text
DOMAIN
ENTERPRISE
AGENT BEHAVIOR
GOVERNANCE
PLATFORM STANDARD
ADAPTER / BINDING
RUNTIME
IMPLEMENTATION HOW
EVALUATION
```

Required safeguards:

```text
professional source authority
→ DOMAIN first

organization-approved source classes
→ ENTERPRISE only if the Case actually needs organization-specific policy

project facts / professional applicability
→ DOMAIN / AGENT

parser / search / index / deterministic rule engine
→ IMPLEMENTATION HOW unless evidence justifies a Governed Seam

Candidate freeze / benchmark separation
→ EVALUATION / GOVERNANCE
```

E2 must not invent Enterprise IAM / policy solely because it is foreseeable.

---

# 13. E2-AB — P-05 Catalyst Compatibility Review

Create:

```text
method/P05_E2_CATALYST_COMPATIBILITY_REVIEW_V0.1.md
```

For every new mechanism decide two independent axes.

## AXIS A — Governance Depth

```text
PRIVATE HOW
or
GOVERNED SEAM
```

Default:

```text
NO NEW GOVERNED SEAM
```

unless real evidence proves stable independent responsibility + observable obligations + replaceability.

Preferred accepted seams remain:

```text
SEAM-01 Professional Project Facts
SEAM-02 Regulation Applicability
SEAM-03 Regulation Evidence
```

If a new Governed Seam is materially necessary:

```text
STOP E2-AB BEFORE IMPLEMENTATION
→ ARCHITECTURE / DESIGN REVIEW
```

## AXIS B — Placement / Generalization

Classify each mechanism:

```text
CASE-LOCAL
EXTENSION
ADAPTER-LOCAL
GENERALIZATION REVIEW CANDIDATE
DEFER
REJECT
```

No Platform Core promotion is authorized.

---

# 14. E2-AB — P-06 Catalyst-Native Reconstruction

Create:

```text
method/P06_E2_CATALYST_NATIVE_RECONSTRUCTION_V0.1.md
```

The E2 design must use Catalyst-native concepts:

```text
Professional Coverage Need
↓
Governed Change Interpretation
↓
Function / Seam / Obligation Impact
↓
Implementation Capability Selection
↓
BREA v0.3 Candidate
↓
Candidate Freeze
↓
Independent Evaluation Contract / Benchmark
↓
Decision Evidence
```

It must identify:

```text
FN impact
SEAM impact
OBL impact
professional fact changes
source changes
implementation capability changes
private HOW vs governed responsibility
expected observable behavior change
expected fail-closed behavior
```

---

# 15. Professional Purpose / FN / SEAM / OBL Gate

The accepted BREA professional purpose is expected to remain stable.

Current accepted responsibilities remain:

```text
FN-01..FN-11
SEAM-01..SEAM-03
OBL-01..OBL-06
```

E2 may extend implementation and bounded semantics inside the accepted responsibilities.

If the selected E2 slice requires:

```text
material professional-purpose expansion
new obligation
new stable Governed Seam
removal / redefinition of accepted FN / SEAM / OBL
```

then:

```text
STOP E2-AB
→ DESIGN REVIEW REQUIRED
```

Do not silently rewrite 01-B/E1 governance contracts.

---

# 16. Technology Selection Gate

Before implementation capability selection, create:

```text
professional/E2_IMPLEMENTATION_CAPABILITY_SELECTION_V0.1.md
```

Decision sequence:

```text
Required observable professional behavior
↓
Current v0.2 deterministic capability
↓
Demonstrated capability gap
↓
Candidate implementation mechanisms
↓
Evidence-based selection
```

Candidate mechanisms may include:

```text
existing deterministic parser
structured table parser
lexical expansion / synonym map
full-text search
cross-clause deterministic resolver
OCR normalization
vector retrieval
RAG
LLM
human review
```

For each selected mechanism record:

```text
why needed
Function owner
private HOW / governed-seam decision
failure mode
replacement path
evaluation method
```

If deterministic/local mechanisms are sufficient:

```text
DO NOT ADD LLM / RAG
```

---

# 17. E2 Professional Change Request

Before Candidate construction create:

```text
change/E2_PROFESSIONAL_CHANGE_REQUEST_V0.1.md
```

It must be a concise natural-language product change derived from P-01..P-06, not from implementation details.

It must include:

```text
current v0.2 limitation
new professional behavior
required evidence / applicability
fail-closed requirement
scope exclusions
Candidate version target
```

This is a product-change input, not implementation authorization.

---

# 18. E2 Change Impact Review

Create:

```text
change/E2_CHANGE_IMPACT_REVIEW_V0.1.md
```

At minimum classify:

```text
Professional purpose
UNCHANGED / MATERIAL CHANGE

FN-01..FN-11
UNCHANGED / EXTENDED / IMPLEMENTATION CHANGE

SEAM-01..03
UNCHANGED / EXTENDED / MATERIAL CHANGE

OBL-01..06
UNCHANGED / EXTENDED / MATERIAL CHANGE

Domain semantics
Enterprise semantics
Agent behavior
Implementation HOW
Evaluation
```

Only `UNCHANGED` / bounded `EXTENDED` / implementation changes may proceed under E2.

Material change triggers STOP.

---

# 19. E2-B — Candidate N+1 Formation

Target:

```text
case-01.brea @ 0.3-candidate
```

Use the existing E1 Case-local governed change mechanism unless evidence proves it insufficient.

Before copy / overlay, freeze v0.2 Reference evidence:

```text
v0.2 source tree identity
v0.2 deterministic implementation fingerprint
E1 benchmark/evaluation baseline identity
E1 known limitations
```

Required formation behavior:

```text
v0.2 Reference
→ verified baseline
→ authorized change set
→ controlled overlay / construction
→ unchanged file verification
→ v0.3 Candidate
→ structural / conformance self-check
→ construction trace
```

If the change Builder needs a small Case-local enhancement:

```text
allowed only inside E2 write scope
record exact Builder gap
record why existing mechanism was insufficient
no Platform promotion
```

A Builder redesign outside the Case-local mechanism is not authorized.

---

# 20. v0.3 Candidate Professional Requirements

The v0.3 Candidate must demonstrate, before independent benchmark creation:

```text
new selected professional family implemented
source-backed evidence path exists
required project facts represented
professional applicability / explicit non-applicability behavior exists
numeric safety preserved
source fidelity preserved
uncertainty / fail-closed path exists
existing generalized local query behavior preserved
T-C01 / T-C02 / T-C03 regression preserved
```

The implementation must not create one question-specific answer branch per expected evaluation case.

The selected capability must be produced by a reusable mechanism.

---

# 21. Anti-Hardcode / Anti-Fixture Gate Before Freeze

Before Candidate Freeze, perform a source-level anti-hardcode review.

It must assert:

```text
no planned evaluation phrases embedded as dedicated runtime branches
no fixed benchmark question strings in Candidate logic
no question → fixed answer mapping
no source-locator list derived from future hidden benchmark
```

At this point the independent E2-C benchmark must not yet exist in committed or Candidate-visible form.

The anti-hardcode review may use only:

```text
E2 product capability contract
selected professional family
source evidence
construction self-tests
```

not future benchmark cases.

---

# 22. E2 Evaluation Contract — Created Before Freeze, Cases After Freeze

Important distinction:

The **Evaluation Contract** must be defined before Candidate Freeze because the Candidate must know the capability it is expected to satisfy.

The **specific independent Benchmark cases** must be created / revealed only after Candidate Freeze.

Create before freeze:

```text
evaluation/E2_EVALUATION_CONTRACT_V0.1.md
```

Required fields:

```text
target professional capability
observable success behavior
required evidence properties
professional applicability expectations
numeric safety expectations
source fidelity expectations
missing-context behavior
non-applicable behavior
no-reliable-evidence behavior
regression obligations
common shortcuts / invalid success modes
minimum benchmark class requirements
```

The Evaluation Contract must not contain the exact future benchmark cases or Gold answers.

---

# 23. Candidate Freeze Package

After v0.3 formation and construction self-checks pass, create:

```text
freeze/E2_V0_3_CANDIDATE_FREEZE_RECORD_V0.1.json
freeze/E2_V0_3_CANDIDATE_FREEZE_REPORT_V0.1.md
```

Freeze Record must include:

```text
agent_id
candidate_version
parent reference
candidate_tree_sha256
manifest / file hashes
changed file list
unchanged verification
professional source references / SHAs
Evaluation Contract SHA / ref
FN / SEAM / OBL conformance refs
construction self-test refs
created_under_E2_AB_authorization_ref
```

Once Publication Gate 1 is pushed:

```text
BREA v0.3 Candidate source becomes immutable for E2-C.
```

Any post-freeze Candidate code change requires:

```text
new Candidate version / Candidate N+1
```

not amendment of the frozen Candidate.

---

# 24. E2-AB Required Output Package

Write only under:

```text
case-01/01-e-governed-agent-professional-completion/e2-local-professional-coverage-expansion/**
```

Expected Gate-1 package:

```text
method/
  P01_E2_PROBLEM_RECORD_V0.1.md
  P02_E2_MECHANISM_ABSTRACTION_V0.1.md
  P03_E2_ASSUMPTION_REGISTER_V0.1.md
  P04_E2_RESPONSIBILITY_CLASSIFICATION_V0.1.md
  P05_E2_CATALYST_COMPATIBILITY_REVIEW_V0.1.md
  P06_E2_CATALYST_NATIVE_RECONSTRUCTION_V0.1.md

professional/
  E2_PROFESSIONAL_COVERAGE_SELECTION_V0.1.md
  E2_SOURCE_ADMISSION_RECORD_V0.1.md              # only if new source required
  E2_IMPLEMENTATION_CAPABILITY_SELECTION_V0.1.md

change/
  E2_PROFESSIONAL_CHANGE_REQUEST_V0.1.md
  E2_CHANGE_IMPACT_REVIEW_V0.1.md
  E2_CANDIDATE_CHANGE_MANIFEST_V0.1.json

builder/
  <minimum Case-local change Builder changes if needed>
  E2_BUILDER_RUN_REPORT_V0.1.md

candidate/
  brea-v0.3/**

evaluation/
  E2_EVALUATION_CONTRACT_V0.1.md

freeze/
  E2_V0_3_CANDIDATE_FREEZE_RECORD_V0.1.json
  E2_V0_3_CANDIDATE_FREEZE_REPORT_V0.1.md

evidence/
  E2_AB_CONSTRUCTION_TEST_RESULTS.log.txt
  E2_AB_REGRESSION_RESULTS.log.txt
  E2_AB_ANTI_HARDCODE_REVIEW_V0.1.md
  E2_AB_FN_SEAM_OBL_CONFORMANCE_V0.1.md
  E2_AB_PLATFORM_COMPATIBILITY_CHECK_V0.1.md
  E2_AB_REPOSITORY_INTEGRITY_V0.1.md
  E2_AB_EVIDENCE_INDEX_V0.1.md

review/
  CASE_01_E_E2_AB_CANDIDATE_FREEZE_REPORT_V0.1.md
```

No independent Benchmark case file is allowed in Gate 1.

---

# 25. E2-AB Candidate Construction Self-Checks

Before Candidate Freeze, Gate 1 must pass at least:

```text
AB-T01 v0.2 Reference identity / fingerprint exact
AB-T02 protected v0.1 unchanged
AB-T03 selected professional source provenance valid
AB-T04 raw corpus remains local / not GitHub-upstreamed
AB-T05 Professional Change Request exists
AB-T06 Change Impact Review permits bounded change
AB-T07 P-01..P-06 evidence complete
AB-T08 selected implementation capability has evidence-based rationale
AB-T09 v0.3 Agent identity lineage preserved
AB-T10 FN-01..11 conformance
AB-T11 SEAM-01..03 conformance
AB-T12 OBL-01..06 conformance
AB-T13 new professional capability construction self-test passes
AB-T14 E1 generalized local-query regression passes
AB-T15 T-C01 regression passes
AB-T16 T-C02 regression passes
AB-T17 T-C03 regression passes
AB-T18 anti-hardcode source review passes
AB-T19 Platform compatibility passes without Core/Runtime changes
AB-T20 deterministic Candidate fingerprint generated
AB-T21 Evaluation Contract exists without future exact Benchmark cases
AB-T22 no unauthorized path changes
```

These are construction / conformance checks.

They are not the independent E2 professional benchmark.

---

# 26. E2-AB Publication Gate 1

E2-AB execution requires explicit User authorization.

That authorization should permit:

```text
professional coverage selection
P-01..P-06 Method artifacts
new local source admission proof if required
BREA v0.3-candidate formation
construction self-checks
Candidate Freeze
ONE Candidate-Freeze implementation+evidence commit
ONE push to case-01
```

After push:

```text
STOP
→ CHATGPT EXTERNAL CANDIDATE-FREEZE REVIEW
```

E2-C remains unauthorized.

The external Freeze Review must verify at minimum:

```text
professional slice validity
source provenance
P-01..P-06 integrity
v0.2 immutable reference
v0.3 Candidate lineage
FN / SEAM / OBL boundaries
anti-hardcode review
Evaluation Contract vs benchmark-case separation
Candidate fingerprint
Platform / Runtime integrity
```

Only after:

```text
FREEZE REVIEW PASS
+
explicit second User authorization
```

may E2-C begin.

---

# 27. E2-C — Independent Benchmark Creation

E2-C starts from the exact frozen Candidate commit / fingerprint.

Candidate code is read-only.

Create only after freeze:

```text
evaluation/benchmark/E2_PROFESSIONAL_BENCHMARK_V0.1.json
```

and, where useful:

```text
evaluation/benchmark/E2_BENCHMARK_DECISION_STANDARD_V0.1.json
```

The benchmark must be derived from the already frozen Evaluation Contract, not from observed Candidate branch behavior.

The benchmark must record:

```text
benchmark_id
benchmark_version
created_after_candidate_freeze_ref
frozen_candidate_ref
frozen_candidate_tree_sha256
evaluation_contract_ref
case classes
source / professional decision authority for expected behavior
```

---

# 28. E2-C Minimum Benchmark Shape

Minimum benchmark size:

```text
12 cases minimum
```

Required composition:

```text
≥4 supported cases in the new E2 professional family
≥2 natural-language / fact-layout variations of supported behavior
≥2 missing-context / insufficient-facts cases
≥1 non-applicable or wrong-source trap
≥1 unsupported numeric / normative-conclusion trap
≥1 no-reliable-evidence case
≥3 regression cases representing T-C01 / T-C02 / T-C03
```

Cases may satisfy more than one category, but the benchmark report must identify category coverage explicitly.

At least 3 successful supported cases must use formulations not present as exact dedicated runtime branches.

If the selected E2 family introduces professional applicability:

```text
retrieval-only evidence
!= benchmark PASS
```

Professional success requires the exact behavior defined by the Evaluation Contract.

---

# 29. E2-C Benchmark Leakage Controls

After benchmark creation:

```text
Candidate source hash must remain identical to Freeze Record.
```

Required leakage checks:

```text
benchmark exact question strings absent from dedicated Candidate runtime branches
Gold / decision-standard data not imported by Candidate
Candidate files unchanged after freeze
benchmark created at a later commit / publication chronology than Candidate freeze
```

If the Candidate is modified after benchmark reveal:

```text
E2-C INVALID
→ Candidate N+1 required
```

Do not overwrite the frozen Candidate.

---

# 30. E2-C Professional Evaluation

Run the complete frozen Candidate through the same relevant whole-Agent path used by current CASE 01 evidence.

Evaluation must record:

```text
exact Agent id/version
exact frozen Candidate fingerprint
exact Benchmark version / SHA
evaluation runtime / environment identity where relevant
professional source identity
per-case result
per-case evidence / locator
applicability decision
uncertainty status
numeric-safety result
Trace / execution evidence where available
```

No case is considered PASS solely because a matching text fragment was retrieved.

---

# 31. E2-C Professional Evaluation Threshold

E2 final product PASS requires:

```text
ALL mandatory safety / fail-closed cases PASS
ALL T-C01 / T-C02 / T-C03 regression cases PASS
ALL source-fidelity / unsupported-numeric trap cases PASS
new professional-family supported-case success rate >= 80%
overall benchmark success rate >= 85%
no architecture / FN / SEAM / OBL regression
```

Because E2 is still a bounded pilot, the threshold is not a production SLA.

But mandatory safety cases are not score-averaged away.

If a mandatory safety case fails:

```text
E2 PRODUCT PASS = NO
```

---

# 32. E2-C — P-07 Case-Local Proof

Create:

```text
method/P07_E2_CASE_LOCAL_PROOF_V0.1.md
```

It must connect:

```text
selected professional problem
→ source evidence
→ Catalyst-native reconstruction
→ frozen v0.3 Candidate
→ independent benchmark
→ observed professional behavior
```

It must explicitly state what remains Case-local.

---

# 33. E2-C — P-08 Evidence Package

Create:

```text
method/P08_E2_EVIDENCE_REVIEW_PACKAGE_V0.1.md
```

This is not the final external review.

It is the evidence package prepared for ChatGPT external review.

It must index:

```text
source provenance
P-01..P-07 artifacts
Candidate Freeze
Benchmark chronology
benchmark identity
case outputs
FN / SEAM / OBL conformance
anti-hardcode evidence
Platform / Runtime integrity
known limitations
```

ChatGPT external review completes the independent P-08 judgment.

---

# 34. E2-C — P-09 Generalization Gate

Create:

```text
method/P09_E2_GENERALIZATION_GATE_V0.1.md
```

For each observed reusable pattern classify:

```text
CASE-PROVEN
GENERALIZATION CANDIDATE
NOT ENOUGH EVIDENCE
REJECT
```

Candidates may include:

```text
Case-local change Builder mechanism
Candidate Freeze / independent evaluation pattern
local professional source admission pattern
professional source abstraction
```

A `GENERALIZATION CANDIDATE` must state its qualifying basis:

```text
second independent need
or
structural / cross-boundary necessity
```

No Platform promotion occurs in E2.

---

# 35. E2-C Required Output Package

Expected Gate-2 package:

```text
evaluation/benchmark/
  E2_PROFESSIONAL_BENCHMARK_V0.1.json
  E2_BENCHMARK_DECISION_STANDARD_V0.1.json      # if used

evaluation/results/
  E2_PROFESSIONAL_BENCHMARK_RESULTS_V0.1.json
  E2_PROFESSIONAL_BENCHMARK_RESULTS_V0.1.md
  E2_BENCHMARK_LEAKAGE_CHECK_V0.1.md
  E2_FROZEN_CANDIDATE_INTEGRITY_CHECK_V0.1.md

method/
  P07_E2_CASE_LOCAL_PROOF_V0.1.md
  P08_E2_EVIDENCE_REVIEW_PACKAGE_V0.1.md
  P09_E2_GENERALIZATION_GATE_V0.1.md

evidence/
  E2_C_PROFESSIONAL_EVALUATION_RESULTS.log.txt
  E2_C_PLATFORM_REGRESSION_RESULTS.log.txt
  E2_C_REPOSITORY_INTEGRITY_V0.1.md
  E2_C_EVIDENCE_INDEX_V0.1.md

review/
  CASE_01_E_E2_FINAL_EXECUTION_REPORT_V0.1.md
  CASE_01_E_E3_ENTRY_BOUNDARY_V0.1.md
  E2_METHOD_VALIDATION_CANDIDATE_V0.1.md
```

Candidate source must not change in Gate 2.

---

# 36. Method Validation Candidate

After successful E2-C execution, generate:

```text
review/E2_METHOD_VALIDATION_CANDIDATE_V0.1.md
```

It must answer for P-01..P-09:

```text
what artifact proves the step
what Case decision occurred
what external mechanism was used
what was reconstructed Catalyst-natively
what was not inherited
what evidence proves usefulness
what assumptions remained
what failed / remained uncertain
```

Allowed self-reported state:

```text
READY FOR EXTERNAL METHOD VALIDATION REVIEW
```

Forbidden self-reported state:

```text
CASE 01 VALIDATED METHOD
```

Only ChatGPT external Method review may recommend that status.

---

# 37. Platform Compatibility / Execution Boundary

E2 should preserve the existing Platform-compatible execution shape.

No modification is authorized to:

```text
platform_standard/**
agent_runtime/**
RuntimeAdapter
enterprise_extensions/**
main
```

If v0.3 cannot execute through current compatible mechanics without changing those layers:

```text
STOP
→ ARCHITECTURE REVIEW REQUIRED
```

A Case-local E2 adapter/helper may be added only within the E2 write scope if required and if it does not take ownership of professional meaning.

---

# 38. Domain / Enterprise Boundary

E2 is expected to be primarily a Domain / Agent professional-completion Stage.

Domain may need to represent:

```text
new professional facts
new applicability semantics
new source authority / source limitations
OCR-normalized vs raw evidence distinction
new conditional-rule interpretation
```

Enterprise remains minimal unless a genuine organization-specific requirement appears.

Do not create:

```text
Enterprise source policy
network approval engine
IAM
RBAC
retention platform
```

without real E2 evidence.

If a source-trust decision is actually organization-specific rather than professional:

```text
record COMPOSED Domain × Enterprise ownership
```

Do not force it into one layer.

---

# 39. Raw Corpus Boundary

Raw professional corpus remains:

```text
LOCAL
READ-ONLY
OUTSIDE GITHUB
```

E2 may commit only:

```text
source identity
SHA256
metadata
provenance record
quality assessment
locator/evidence excerpts required for evaluation where legally/operationally appropriate
```

Do not upstream full raw regulation documents.

---

# 40. E2 Allowed Write Scope

During authorized E2 phases, writes are limited to:

```text
case-01/01-e-governed-agent-professional-completion/e2-local-professional-coverage-expansion/**
```

Read-only references may include:

```text
01-B Governed Definition
01-C formation evidence
01-D admission / binding evidence
E1 v0.2 Candidate and evidence
accepted Method
accepted Platform / Runtime source at main baseline
local regulation sources / Legacy evidence where admitted by the Stage gate
pinned external reference files for research evidence
```

---

# 41. Forbidden Mutations

E2 may not modify:

```text
BREA v0.1 Candidate
BREA v0.2 Candidate
01-B / 01-C / 01-D / E1 accepted evidence
Catalyst main
Platform Standard / Platform Core
Runtime / RuntimeAdapter
Enterprise extensions
root tests / CI
raw regulation corpus
Penguin Harness repository
```

E2 may not create:

```text
Generic Agent Builder Platform
Generic Evaluation Platform
Generic Corpus Platform
Agent Control Plane
IAM / RBAC
Policy Engine
Generic RAG Platform
```

merely because Case-local code becomes useful.

---

# 42. E2-AB Stop Conditions

STOP before Candidate Freeze if any occur:

```text
AB-S01 no materially new professional family can be selected
AB-S02 source provenance / authority is insufficient
AB-S03 raw corpus must be committed to proceed
AB-S04 professional purpose must materially change
AB-S05 new obligation is necessary
AB-S06 new Governed Seam is structurally necessary
AB-S07 Platform Core / Runtime change appears necessary
AB-S08 v0.2 Reference cannot be frozen deterministically
AB-S09 selected technology is not justified by observed capability gap
AB-S10 implementation would revert to one-question-one-hardcode branches
AB-S11 Evaluation Contract already leaks exact future benchmark cases
AB-S12 protected historical Candidate / evidence mutation occurs
AB-S13 main drift occurs
```

On STOP:

```text
record smallest blocker
preserve evidence
no architecture repair
return for review
```

---

# 43. E2-C Stop Conditions

STOP evaluation if any occur:

```text
C-S01 Candidate Freeze Review not PASS
C-S02 second User authorization missing
C-S03 frozen Candidate source/hash changed
C-S04 Benchmark existed before Candidate Freeze publication
C-S05 Benchmark / Gold leaked into Candidate implementation
C-S06 evaluation runtime cannot identify exact frozen Candidate
C-S07 professional source identity cannot be resolved
C-S08 mandatory safety case fails because of infrastructure uncertainty that cannot be distinguished from Agent behavior
C-S09 Candidate repair is needed after benchmark reveal
C-S10 Platform Core / Runtime mutation occurs
C-S11 main drift occurs
```

If Candidate repair is needed:

```text
retain E2-C evaluation as evidence
→ do not edit v0.3 in place
→ propose Candidate N+1 under new authorization
```

---

# 44. E2 Acceptance Criteria — Product

Final external E2 product PASS requires:

```text
AC-E2-P01 one new materially different professional family selected
AC-E2-P02 authoritative local evidence / source gate passed
AC-E2-P03 v0.2 immutable Reference preserved
AC-E2-P04 v0.3 Candidate formed as N+1
AC-E2-P05 professional purpose preserved
AC-E2-P06 FN-01..11 conformance preserved
AC-E2-P07 SEAM-01..03 conformance preserved
AC-E2-P08 OBL-01..06 conformance preserved
AC-E2-P09 new professional behavior is reusable, not fixture-specific
AC-E2-P10 applicability != retrieval boundary preserved
AC-E2-P11 numeric safety preserved
AC-E2-P12 source fidelity preserved
AC-E2-P13 fail-closed behavior preserved
AC-E2-P14 E1 generalized local-query regression passes
AC-E2-P15 T-C01 / T-C02 / T-C03 regression passes
AC-E2-P16 Candidate frozen before specific benchmark creation
AC-E2-P17 independent benchmark >=12 cases with required category coverage
AC-E2-P18 all mandatory safety cases PASS
AC-E2-P19 new-family supported-case success >=80%
AC-E2-P20 overall benchmark success >=85%
AC-E2-P21 Platform / Runtime unchanged
AC-E2-P22 raw corpus not committed
AC-E2-P23 main unchanged
```

---

# 45. E2 Acceptance Criteria — Method

E2 may be eligible for Method Validation Review only if:

```text
AC-E2-M01 P-01 artifact present and technology-neutral
AC-E2-M02 P-02 mechanism abstraction traces to pinned external source
AC-E2-M03 P-03 assumptions explicit
AC-E2-M04 P-04 responsibility classification explicit
AC-E2-M05 P-05 two-axis compatibility decision explicit
AC-E2-M06 P-06 Catalyst-native reconstruction explicit
AC-E2-M07 P-07 Case-local proof evidence complete
AC-E2-M08 P-08 external-review package complete
AC-E2-M09 P-09 generalization gate complete
AC-E2-M10 external implementation shape not inherited as Catalyst architecture
AC-E2-M11 Candidate Freeze / Benchmark isolation mechanism materially used
AC-E2-M12 observed evidence shows the mechanism improved evaluation integrity
AC-E2-M13 Method did not override Stage / architecture authority
AC-E2-M14 E2 final external review confirms the chain
```

Even if all pass:

```text
Method status change requires separate explicit external Method review.
```

---

# 46. E2 Verdict Model

After E2-C, DeepSeek may report one:

```text
READY FOR E2 EXTERNAL REVIEW

PROFESSIONAL CAPABILITY FAILURE

EVALUATION INVALID / LEAKAGE DETECTED

ARCHITECTURE REVIEW REQUIRED

CANDIDATE N+1 REPAIR REQUIRED
```

ChatGPT external review decides:

```text
A. EVIDENCE-BACKED PASS / CLOSED
B. TARGETED REPAIR / NEW CANDIDATE REQUIRED
C. ARCHITECTURE REVIEW REQUIRED
D. FAIL
```

Method validation status is reviewed separately after product/evidence closure.

---

# 47. E2 Publication Rules

## Gate 1 — E2-AB

Requires first explicit User authorization.

Authorized publication, if granted:

```text
ONE Candidate-Freeze implementation+evidence commit
+
ONE push to case-01
+
STOP
```

No benchmark-case publication in Gate 1.

## Gate 2 — E2-C

Requires:

```text
Candidate Freeze external review PASS
+
second explicit User authorization
```

Authorized publication, if granted:

```text
ONE independent-evaluation evidence commit
+
ONE push to case-01
+
STOP
```

Candidate code must not change in Gate 2.

No PR to `main`.

Post-push Candidate repair requires a new Candidate / new authorization.

---

# 48. E2-AB Final Report Shape

After Gate 1 execution, DeepSeek must report:

```text
E2-AB STATUS
READY FOR CANDIDATE-FREEZE EXTERNAL REVIEW / BLOCKED

CASE-01 HEAD INPUT
<sha>

METHOD REF
a5e7b0b9384f8108d5af22bfe9fea317d8e60cee

REFERENCE
case-01.brea @ 0.2-candidate

TARGET CANDIDATE
case-01.brea @ 0.3-candidate

SELECTED PROFESSIONAL FAMILY
<summary>

SOURCE PATH
A / B

NEW SOURCE
NONE or <id/title/SHA>

P-01..P-06
PASS / BLOCKED per step

PROFESSIONAL PURPOSE CHANGE
NO / YES

NEW GOVERNED SEAM
NO / YES

NEW OBLIGATION
NO / YES

IMPLEMENTATION CAPABILITY
<summary>

V0.2 REFERENCE INTEGRITY
PASS / FAIL

V0.3 FORMATION
PASS / FAIL

FN / SEAM / OBL
PASS / FAIL

CONSTRUCTION SELF-CHECKS
N/N PASS

ANTI-HARDCODE
PASS / FAIL

PLATFORM COMPATIBILITY
PASS / FAIL

RAW CORPUS COMMITTED
NO / YES

CANDIDATE TREE SHA256
<sha>

EVALUATION CONTRACT
GENERATED

SPECIFIC INDEPENDENT BENCHMARK CASES
NOT CREATED

GATE-1 COMMIT
<sha>

MAIN
UNCHANGED / CHANGED

E2-C
NOT AUTHORIZED

FINAL
READY FOR CANDIDATE-FREEZE EXTERNAL REVIEW
```

STOP.

---

# 49. E2-C Final Report Shape

After Gate 2 execution, DeepSeek must report:

```text
E2-C STATUS
READY FOR E2 EXTERNAL REVIEW / FAILED / INVALID

FROZEN CANDIDATE
case-01.brea @ 0.3-candidate

FREEZE COMMIT
<sha>

FROZEN TREE SHA256
<sha>

CANDIDATE CHANGED AFTER FREEZE
NO / YES

BENCHMARK
E2_PROFESSIONAL_BENCHMARK_V0.1

BENCHMARK CREATED AFTER FREEZE
YES / NO

BENCHMARK CASES
N

NEW-FAMILY SUPPORTED SUCCESS
X/Y

OVERALL
X/Y

MANDATORY SAFETY CASES
ALL PASS / FAIL

T-C01 / T-C02 / T-C03
PASS / PASS / PASS or failures

LEAKAGE CHECK
PASS / FAIL

SOURCE FIDELITY
PASS / FAIL

NUMERIC SAFETY
PASS / FAIL

FAIL-CLOSED
PASS / FAIL

P-07
PASS / FAIL

P-08 PACKAGE
COMPLETE / INCOMPLETE

P-09
COMPLETE / INCOMPLETE

METHOD VALIDATION CANDIDATE
GENERATED / NOT GENERATED

PLATFORM CORE CHANGE
NO / YES

RUNTIME CHANGE
NO / YES

RAW CORPUS COMMITTED
NO / YES

MAIN
UNCHANGED / CHANGED

GATE-2 COMMIT
<sha>

E3
NOT AUTHORIZED

BREA v0.3 ADMISSION / BINDING
NOT AUTHORIZED

FINAL
READY FOR E2 EXTERNAL REVIEW
```

STOP.

---

# 50. Product Mainline After E2

E2 does not decide later stages automatically.

Expected product sequence remains:

```text
E1
Generalized Local Evidence Query
CLOSED

E2
Local Professional Coverage Expansion
CURRENT

later
Local-first → Web Supplement

later
LLM / RAG / Loop / Memory
only where actual product evidence justifies them

later
broader independent professional evaluation

later
usable professional Agent acceptance
```

BREA v0.3 Admission / Binding is also a separate governance decision.

E2 success does not automatically admit v0.3.

---

# 51. Current Authorization State

```text
ACCEPTED CONSTRUCTION METHOD
YES — FOR CASE 01 USE

METHOD FULL END-TO-END VALIDATION
PENDING E2

E2 STAGE PLAN
CLOSED INTO THIS SPEC

E2 STAGE SPEC
ACCEPTED

E2-AB EXECUTION
NOT AUTHORIZED YET

E2-C EXECUTION
NOT AUTHORIZED

BREA v0.2
REFERENCE / READ-ONLY

BREA v0.3
NOT YET FORMED

BREA v0.3 ADMISSION / BINDING
NOT AUTHORIZED

E3
NOT AUTHORIZED

PLATFORM CORE CHANGE
FORBIDDEN

RUNTIME / RUNTIME ADAPTER CHANGE
FORBIDDEN

MAIN CHANGE
FORBIDDEN
```

# **E2 SPEC VERDICT — ACCEPTED / READY FOR E2-AB EXPLICIT EXECUTION AUTHORIZATION**
