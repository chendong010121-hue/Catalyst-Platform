# Catalyst Platform — History Map

Operational `main` is intentionally not an archive. Closed Stages, superseded candidates, old Handoffs, one-off audit reports, Case work, and pre-Operational proof fixtures remain recoverable through Git history, closed PRs, and frozen refs.

This page is navigation only. Historical material does not become current authority merely because it is linked here.

---

## 1. Current rule

For current state, use:

```text
CATALYST_OPERATIONAL_BASELINE_V1.md
GitHub main
active tests / CI
```

For historical reasoning, use this map to locate exact refs and understand their disposition.

A historical branch may be retained because it carries evidence or lineage. Branch existence does **not** mean current authorization.

---

## 2. Full pre-Operational V1 repository snapshot

```text
branch: archive/pre-operational-v1
commit: 3986236db1dc66ee0bc78ac2a4264792d4a8f5fb
status: FROZEN HISTORICAL SNAPSHOT
```

This ref preserves the complete pre-consolidation tree: root Stage Specs, comparative audits, integration decision fixtures, Runtime implementation notes, V0.2 repair records, old Handoffs, the Capability-Preserving Evolution promotion review, and pre-Operational test campaigns.

Use it when reconstructing historical decisions. Do not copy those files back into the active root merely for convenience.

---

## 3. Case01

```text
branch: case-01
head:   232d6837647c68670fba3f3b2faf7ec1fac73f0a
status: FROZEN HISTORICAL PRODUCT / EVALUATION LINEAGE
```

Role:

- historical Building Regulation / BREA product-development evidence;
- professional Capability Evaluation and failure-attribution lineage;
- source of bounded fail-closed numeric-safety learning.

Operational `main` does not execute Case01 and does not treat Case01 as Platform architecture authority.

Current durable value is navigated through:

```text
assets/knowledge/FAIL_CLOSED_NUMERIC_SAFETY_V0.1.json
docs/governance/CATALYST_PHASE_2_CLOSURE_REVIEW_V0.1.md
```

---

## 4. Case01 Harness Trial 01 — independent historical proof

```text
branch: case-01-harness-trial-01
head:   3de91bbcf8c74cc9cfd96dd4eb40bbff230c660b
status: HISTORICAL PASS EVIDENCE / NOT CURRENT PLATFORM IMPLEMENTATION
```

This branch contains one independent Trial commit not present on the final `case-01` ref.

The Trial proved, at its declared scope:

```text
bounded authorized Harness task
real DeepSeek provider execution
authorized file write only
fixed verification command
credential separation from tool subprocess
11-test governance verifier PASS
representative professional regression PASS
```

Final historical verdict:

```text
CASE01_HARNESS_TRIAL_01_PASS
```

The Trial did **not** authorize Platform promotion, admission, binding, a permanent built-in Harness, or merge to current Platform architecture. Its value is execution/evidence lineage, not current implementation authority.

---

## 5. Case02

```text
branch: case-02
head:   336f8e6f28c1569e5c53f245daaa3ee8a197f33d
external source: ShenSeanChen/waku-agent@8328f567ab52d07921445cb40feed23cbc5ea2ad
status: FROZEN EXTERNAL-AGENT / HARVEST LINEAGE
```

Role:

- historical external-Agent understanding / decomposition evidence;
- governed harvested mechanism catalog;
- proof that useful capability/mechanism knowledge may survive while the source Agent remains disposable.

Current durable value is navigated through:

```text
assets/knowledge/WAKU_RETRIEVAL_GATED_MEMORY_V0.1.json
```

---

## 6. Historical Platform Harness proof branch

```text
branch: platform-harness
head:   089eb3f3350aeedacea104625b3ac003cfa267b7
status: HISTORICAL MECHANISM / EXECUTION EVIDENCE
```

This branch contains historical Minimum Harness and Harness Environment Infrastructure work that was never promoted as current Platform identity.

Useful evidence includes, at its declared scope:

```text
bounded Harness Session / Workspace behavior
replaceable model-provider invocation
ExecutionPolicy / ApprovalPolicy separation
credential-source separation
provider credential excluded from tool subprocess environment
fixed verification command identity
live DeepSeek execution
deterministic verification
```

The historical v0.2 candidate recorded:

```text
HARNESS_ENVIRONMENT_INFRASTRUCTURE_V0_1_PASS
```

Durable architectural learning from this work has been absorbed into current principles and methods where justified: Harness remains replaceable; external Harnesses are legitimate HOW candidates; governed production and evidence matter more than ownership of one Harness implementation.

What is **not** inherited as current authority:

```text
"Catalyst must permanently own this concrete Harness implementation"
old Stage authorization
old package/API shape
old product framing
```

The branch `platform-harness-governance-note` at `7d801bb7d84b34fb52900f58b27e9a0d1cbb1c07` is fully contained by the later `platform-harness` branch and carries no unique lineage beyond that retained ref.

---

## 7. Capability / Harvest Philosophy source branch

```text
branch: docs/capability-harvest-philosophy-v0.1
head:   f132a573ff7eb198f492db8ebb882f0a6efb4221
status: HISTORICAL SEDIMENTATION SOURCE / CURRENT TEXT SUPERSEDED
```

This branch is an early source of the accepted Capability / Harvest design philosophy. It diverges textually from current `main` because the philosophy was later expanded, audited, and formally marked:

```text
ACCEPTED / SEDIMENTED
```

Current interpretive authority is:

```text
docs/CATALYST_CAPABILITY_HARVEST_DESIGN_PHILOSOPHY_V0.1.md
```

Do not treat wording that exists only on the historical source branch as stronger than the accepted current philosophy.

---

## 8. Capability-Preserving Evolution — PR #15

```text
PR:           #15 — docs: formalize capability-preserving evolution
status:       MERGED / CLOSED
merge commit: 3986236db1dc66ee0bc78ac2a4264792d4a8f5fb
```

PR #15 is not missing. Its stable result is now carried by current authority:

```text
ARCHITECTURE.md
→ stable capability-preserving evolution principle

platform-harness/skills/capability-optimization/SKILL.md
→ replaceable implementation-evolution method (v2)
```

The original Promotion Review remains historical evidence in the pre-Operational snapshot / PR lineage rather than a third active architecture layer.

---

## 9. Phase 2

Current closure record is intentionally retained in active governance because it is the accepted real-Capability adoption evidence chain:

```text
docs/governance/CATALYST_PHASE_2_CLOSURE_REVIEW_V0.1.md
```

Status:

```text
PHASE 2 COMPLETE — PASS
```

The old `review/phase-2-closure` branch is fully contained by current `main`; it is not an additional authority surface.

---

## 10. Superseded PR #1 / RuntimeDomain branch

Historical PR #1 (`RuntimeDomain identity closure`) is closed as:

```text
SUPERSEDED / NOT MERGED
```

Its historical branch `ds/runtime-domain-identity-closure` contains unique rejected work relative to current `main`. That is intentional historical failure/decision evidence, not a missing merge.

Its RuntimeDomain approach is not current architecture or implementation authority.

This preserves the real historical failure/decision record instead of rewriting history to look uniformly successful.

---

## 11. Rejected Minimum Operational / Phase 3 candidate

```text
branch: stage/catalyst-minimum-operational-v1
head:   d43c2b1913a4f3952bcc078726ed2df79895ba6b
status: REJECTED / DO NOT MERGE
```

This branch contains the over-expanded candidate that proposed:

```text
Capability Visibility Index V0.2
Shared Evidence Handoff
Capability Assimilation Skill
large Minimum Operational / Phase 3 Stage
```

A later full-repository responsibility audit found that these proposals duplicated or widened already-owned responsibilities. The candidate was therefore rejected rather than repaired into current architecture.

Its value is **negative design lineage**:

> preserve the goal, discard an unnecessary implementation/design candidate.

Do not revive these artifacts merely because the branch still exists.

---

## 12. Branch disposition audit

A final post-Operational audit compared the remaining development refs against current `main`.

### 12.1 Fully contained in current `main`

The following audited refs have `ahead_by = 0` relative to current `main`; they contain no unique commit that needs to be merged back:

```text
chore/repository-governance-bootstrap
docs/architecture-v2.3
docs/capability-preserving-evolution-v0.1
docs/catalyst-platform-identity
docs/maf-v1-governance-closure
docs/platform-standard-v0.1-closure
docs/readme-v1
ds/cancellation-timeout-mainline-alignment
ds/enterprise-extension-pilot-v0.1
ds/platform-standard-core-v0.1
release/catalyst-operational-v1
review/phase-2-closure
stage/capability-contract-conformance-v0.1
stage/catalyst-minimum-usable-v0.2
stage/catalyst-platform-integration-v0.1
```

After PR #17, `chore/final-operational-v1-hygiene` is also merged into current `main` and carries no independent current authority.

These branch refs are administrative leftovers only. Removing them later does not remove unique accepted Platform value.

### 12.2 Unique historical refs intentionally retained for lineage

```text
archive/pre-operational-v1
case-01
case-01-harness-trial-01
case-02
platform-harness
docs/capability-harvest-philosophy-v0.1
ds/runtime-domain-identity-closure
stage/catalyst-minimum-operational-v1
```

Their disposition is explicitly documented above. None is current authorization.

### 12.3 Contained by another retained historical ref

```text
platform-harness-governance-note
→ contained by platform-harness
→ no separate unique lineage requirement
```

---

## 13. Historical branch policy

Historical development branches may remain as Git refs while they are useful for lineage, but they are not active work merely because they exist.

A branch name such as:

```text
docs/*
ds/*
stage/*
review/*
release/*
case-*
```

must not be interpreted as current authorization.

The repository should have no open historical PRs competing with current work. Current work requires an explicitly active branch / task / Stage.

Branch deletion is administrative hygiene only after the relevant commit/PR/evidence identity is safely preserved. Deleting or retaining a historical ref must not change current Platform semantics.

Where a branch contains unique historical evidence and no equivalent permanent tag/closed-PR ref has been confirmed, preservation is safer than destructive cleanup.

---

## 14. Historical evidence rule

When investigating why the current system looks the way it does:

```text
current authority / Operational Baseline
→ identify the responsibility or claim
→ follow explicit evidence / lineage ref
→ inspect exact historical commit / closed PR / frozen branch when needed
```

Do not use filename recency, numbering, branch existence, or an old `Status:` line to infer current authorization.

---

## 15. Final interpretation

After this lineage audit:

```text
main
= current accepted Platform

historical branches
= evidence / rejected-candidate / superseded-design lineage only

open PR
= active work only
```

No audited historical branch is a hidden current architecture authority or an unmerged accepted Platform feature.
